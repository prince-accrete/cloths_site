# Copy to .env.local and fill in. Safe to commit — contains no secrets.
MONGODB_URI="mongodb+srv://princeuser:V5LmTXRjW_g8i9z@<cluster>.mongodb.net/purepath?retryWrites=true&w=majority"

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
BETTER_AUTH_SECRET="<32-random-bytes>"
BETTER_AUTH_URL="http://localhost:3000"
